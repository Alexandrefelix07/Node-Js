// npm init > npm install typescript --D
// npx tsc app.ts   para gerar o arquivo
var a: string = "treinaweb";
var b: number = 123;

function exemplots(numero: number, texto: string) {
  console.log(texto, numero);
}

exemplots(b, a);
